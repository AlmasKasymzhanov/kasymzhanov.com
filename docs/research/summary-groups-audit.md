# Сводки исследований — 12 сентября 2026

Общий стиль распространён на группы показателей и компактные числовые списки в статьях и исследованиях: фон #f5f7fa в светлой теме и #101419 в тёмной, скругление 14px, поля 24px (20px × 18px на телефонах). Строки ResearchFact имеют вертикальные поля 10px без дополнительных межстрочных отступов. Вложенная группа ResearchSummary не получает вторую подложку и повторные поля.

Дополнительно заменена старая разметка в шести карточках ниш для предзаказа, профиле товара в руководстве по предзаказу, вводной сводке электроники, ценовых сценариях WB Fitness и списке конкурентных брендов Hinoko. В Hinoko удалены оставшиеся от прежней таблицы отдельные заголовки BRAND / BAR / SHARE / TAG. Числа и выводы сохранены.

## Проверка

- Обход 48 публичных адресов из реестра статей, исследований и текстовых инструментов: все возвращают HTTP 200. В серверной разметке 28 страниц найдено 66 групп `.research-facts` / `.research-query-list`. Результат: `.cache/summary-groups-audit.json`.
- В Chrome проверены вычисленные стили сводок кемпинга, косметики WB, ниш для предзаказа и «30 ниш»: одинаковые поля и поверхность, без горизонтального переполнения на проверенном десктопном размере.
- В «30 ниш» проверены обе темы и вложенность: внутренний контейнер прозрачен, его поля и отступы равны нулю; отступы строк не складываются. Итоговый вид тёмной темы проверен скриншотом.
- `npx tsc --noEmit`, `node scripts/check-reading-charts.cjs` и `git diff --check` проходят.

Проверка HTTP и серверной разметки охватывает все 48 адресов; визуальная проверка выполнена на перечисленных представительных страницах. В этом проходе мобильный вид не проверялся повторно в браузере. Графики, тепловые карты, тултипы, источники и методология не перекрашивались стилем сводок. Публикация не выполнялась.
